extends: electron-builder.yml

# This override is consumed only by the protected Community release workflow.
# Ordinary local packages continue to use electron-builder.yml, where
# notarization and DMG signing remain explicitly disabled.
mac:
  notarize: true

dmg:
  sign: true
